node_modules/
dist/
*.tsbuildinfo
.turbo/
.env
.env.*
!.env.example
*.log
coverage/
.DS_Store
Thumbs.db

# Build artifacts that shouldn't be checked in
*.tgz

# Local check / audit dirs
.check/

# Pipeline subrepos still have their own .git histories — exclude entirely.
# Each pipeline server lives in its own GitHub repo (theYahia/<name>-mcp).
/pipeline/

# Skills and content live in separate repos (theYahia/mcp-skills, etc.)
/skills/
/content/

# Local artifact pkg dir
/package/

# MCPB bundles — сборочный артефакт, пересобирается scripts/build-mcpb.mjs
mcpb/*.mcpb
