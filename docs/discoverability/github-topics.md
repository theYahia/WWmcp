# GitHub topics for theYahia/WWmcp

Settings → Topics (max 20). These drive `topic:<name>` searches, the GitHub trending page, and SEO.

```
mcp
mcp-server
mcp-servers
model-context-protocol
claude
anthropic
ai-agents
llm
typescript
monorepo
turborepo
russia
cis
mena
emerging-markets
payments
crm
e-commerce
```

## Why these specifically

- `mcp`, `mcp-server`, `mcp-servers`, `model-context-protocol` — primary discovery filter for anyone hunting MCP servers. Cover all common spellings.
- `claude`, `anthropic` — searched by users browsing for compatible tools.
- `ai-agents`, `llm` — broader AI audience.
- `typescript`, `monorepo`, `turborepo` — devs filtering by tech stack.
- `russia`, `cis`, `mena`, `emerging-markets` — geographic differentiator. Critical because this is what makes WWmcp unique vs the official MCP catalog.
- `payments`, `crm`, `e-commerce` — top categories by server count.

## After setting topics

Verify with:

```bash
gh search repos --topic mcp-server --owner theYahia
```

The repo should appear within a minute.
