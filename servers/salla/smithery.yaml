name: salla-mcp
description: MCP server for Salla e-commerce platform — 22 tools for products, categories, brands, orders, customers, coupons, branches, store (Saudi Arabia)
icon: https://raw.githubusercontent.com/theYahia/WWmcp/main/servers/salla/assets/icon.svg
startCommand:
  type: stdio
  configSchema:
    type: object
    required:
      - SALLA_ACCESS_TOKEN
    properties:
      SALLA_ACCESS_TOKEN:
        type: string
        description: "OAuth 2.0 access token from Salla (https://salla.partners/)"
  commandFunction: |-
    (config) => ({
      command: "npx",
      args: ["-y", "@theyahia/salla-mcp"],
      env: { SALLA_ACCESS_TOKEN: config.SALLA_ACCESS_TOKEN }
    })
